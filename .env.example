# Lead notification emails (POST /api/demo) — uses https://resend.com
# RESEND_API_KEY=re_xxxxxxxx
# Optional verified sender (defaults to Resend onboarding; verify your domain for production)
# RESEND_FROM=FindFoundFast <noreply@yourdomain.com>
# Optional inbox override (defaults in code to your notification address)
# LEAD_INBOX_EMAIL=
