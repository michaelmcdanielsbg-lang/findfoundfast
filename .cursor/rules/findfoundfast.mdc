---
description: FindFoundFast product context — flows, domain, stack, brand (read AGENTS.md for full brief)
alwaysApply: true
---

# FindFoundFast

- **Site:** findfoundfast.com — timed **photo + gate-code links** for deliveries/guests (no driver app).
- **Flows:** (1) **Property manager** — one-time building setup, 4 photos, one permanent link per building. (2) **Resident** — 5-char code, pick timer, copy timed link. (3) **Driver/guest** — tap link, see steps + codes; timer ends → link dies.
- **Marketing routes:** `/` minimal landing; `/how-it-works` tabs (**manager** default, then resident, driver).
- **Stack:** Next.js App Router, Tailwind (`fff` brand colors in `tailwind.config.js`), Supabase, Vercel. Fonts: DM Sans + Space Mono (`app/layout.js`).
- **Voice:** bold, fun, anti-frustration; taglines include “Google Maps for your food and your homies” / “FFFliping Cool”.

**Full product brief, tables, and editor notes:** repo root `AGENTS.md`.
